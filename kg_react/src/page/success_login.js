import React from 'react';

const Successful = () => {
    const msg = 'Successfully Login';

    return (
        <div>
            <h1>{msg}</h1>

        </div>
    );
};

export default Successful;
